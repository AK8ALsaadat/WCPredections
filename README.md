# wcpredections-dedupe

Small utility to deduplicate match records and remove matches without lineup information.

Usage

- Run tests:

```bash
node test/run-tests.js
```

- Sample run:

```bash
node src/index.js
```
